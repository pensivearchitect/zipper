var uid = require('uid');
var htmlfile = require('filesubmitted');
var gs = require('gs');

function storevalue(htmlfile) {
	uid.forEach(function (htmlfile) {
		gs.writeFile( htmlfile, request (err, providedfile){};)
	})
};


